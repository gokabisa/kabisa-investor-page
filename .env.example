# Copy to .env and adjust per environment. Used by build.sh for local builds.
# In Vercel, set API_BASE in the project's Environment Variables instead.
# Host only — do NOT include /api/v1 (that's added in code).
API_BASE=https://investors-api.staging.gokabisa.com
